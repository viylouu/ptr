// demo.ptr

lib stdio
lib stdif
lib stdmem

conc :: {
    halloc main :: &loc(2)
    conc :: {
        salloc hello :: &string("helloo douglas douglas")
        jmp print :: [hello]
    }

    jmp main :: []

    end :: {
        jmp free :: [main]
    }
}
